% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics_tfi.R
\name{compute_tfi_simple}
\alias{compute_tfi_simple}
\title{Compute the Temporal Fragility Index (TFI)}
\usage{
compute_tfi_simple(effect_series, perturb_seq = seq(0, 2, by = 0.01))
}
\arguments{
\item{effect_series}{Numeric vector of observed effects over time
(e.g., DII values, gap values). The trend is estimated as the slope of
a simple OLS regression of \code{effect_series} on a unit time index
(\verb{1, 2, ..., T}). \code{NA} values are removed before estimation.}

\item{perturb_seq}{Numeric vector of perturbation values to evaluate.
Must be non-negative. Defaults to \code{seq(0, 2, by = 0.01)}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{tfi}}{Numeric scalar. The minimum perturbation that nullifies
the trend, or \code{Inf} if none in \code{perturb_seq} does so.}
\item{\code{observed_slope}}{Numeric scalar. OLS slope of \code{effect_series}
on the time index.}
\item{\code{sensitivity_curve}}{Data frame with columns \code{perturbation} and
\code{adjusted_effect} (the slope under each perturbation value).}
\item{\code{summary_table}}{One-row data frame with columns \code{Metric} and
\code{Value}, summarizing the observed slope, TFI, and interpretation.}
}
}
\description{
The TFI estimates the minimum amount of hidden bias (modeled as a scalar
attenuation parameter \eqn{u}) required to nullify a longitudinal trend
conclusion. The observed trend is summarized as the OLS slope of
\code{effect_series} on time index. Under perturbation \eqn{u}, the adjusted
slope is:
}
\details{
\deqn{\hat{\beta}(u) = \hat{\beta}_{obs} \times (1 - u)}

The TFI is the smallest \eqn{u \ge 0} such that \eqn{\hat{\beta}(u) \le 0}
(for positive slopes) or \eqn{\hat{\beta}(u) \ge 0} (for negative slopes).
If no such \eqn{u} exists within \code{perturb_seq}, TFI is returned as \code{Inf},
indicating a highly robust conclusion.

This is an intentionally accessible, first-generation operationalization
of temporal robustness. Future versions will support perturbation models
based on E-values, ITCV (impact threshold for a confounding variable), and
simulation-based tipping-point approaches.
}
\examples{
# Upward drift trend - moderately robust
dii_vals <- c(0.05, 0.10, 0.14, 0.19, 0.25)
result <- compute_tfi_simple(dii_vals)
result$tfi
result$summary_table

# Flat trend - TFI is 0
compute_tfi_simple(c(0.1, 0.1, 0.1, 0.1))$tfi
}
\references{
Hait, S. (2025). \emph{RobustFlow: Robustness and drift auditing for
longitudinal decision systems}. R package version 0.1.0.
}
