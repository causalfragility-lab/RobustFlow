% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_paths.R
\name{compute_transition_matrix_all}
\alias{compute_transition_matrix_all}
\title{Compute the pooled transition matrix across all individuals}
\usage{
compute_transition_matrix_all(data, id, time, decision)
}
\arguments{
\item{data}{A data frame in long format.}

\item{id}{Character scalar. Individual identifier variable name.}

\item{time}{Character scalar. Time variable name.}

\item{decision}{Character scalar. Decision variable name.}
}
\value{
An integer matrix of transition counts with named rows (from-state)
and columns (to-state). Returns a \eqn{0 \times 0} matrix if no
transitions can be extracted.
}
\description{
Pools all consecutive decision pairs (from time \eqn{t} to \eqn{t+1})
across all individuals and returns a matrix of transition counts.
}
\examples{
df <- data.frame(
  id   = rep(1:3, each = 3),
  time = rep(1:3, times = 3),
  dec  = c(0L, 1L, 1L, 1L, 0L, 1L, 0L, 0L, 1L)
)
compute_transition_matrix_all(df, "id", "time", "dec")
}
