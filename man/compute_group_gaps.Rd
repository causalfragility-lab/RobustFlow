% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics_bai.R
\name{compute_group_gaps}
\alias{compute_group_gaps}
\title{Compute group-specific trajectories and disparity gaps over time}
\usage{
compute_group_gaps(data, time, decision, group, focal_value = 1)
}
\arguments{
\item{data}{A data frame in long format.}

\item{time}{Character scalar. Name of the time variable.}

\item{decision}{Character scalar. Name of the decision or outcome variable.}

\item{group}{Character scalar. Name of the grouping variable.}

\item{focal_value}{Numeric or character scalar. The decision value treated
as the "event" when computing group rates (e.g., \code{1} for a binary
at-risk indicator). Default \code{1}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{long_format}}{Data frame with columns \code{time}, \code{group}, and \code{rate}
(proportion of the focal event within each group-time cell).}
\item{\code{gap_df}}{Data frame with columns \code{time} and \code{gap}
(Group 1 rate minus Group 2 rate, where groups are the first two
alphabetically sorted levels). \code{gap} is \code{NA} when fewer than two
group levels are present.}
\item{\code{gap}}{Numeric vector of gap values ordered by time (convenience
accessor for \code{\link[=compute_bai]{compute_bai()}}).}
\item{\code{group_levels}}{Character vector of all group levels found.}
}
}
\description{
Aggregates the focal-event rate for each group at each time point and
computes the pairwise gap between the first two levels of the group
variable (sorted alphabetically).
}
\examples{
set.seed(1)
df <- data.frame(
  id    = rep(seq_len(60), each = 3),
  time  = rep(seq_len(3), times = 60),
  dec   = sample(0:1, 180, replace = TRUE),
  grp   = rep(c("Low", "High"), each = 90)
)
gaps <- compute_group_gaps(
  data        = df,
  time        = "time",
  decision    = "dec",
  group       = "grp",
  focal_value = 1
)
gaps$gap_df
gaps$group_levels
}
