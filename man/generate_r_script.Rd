% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_export.R
\name{generate_r_script}
\alias{generate_r_script}
\title{Generate a reproducible R analysis script}
\usage{
generate_r_script(
  id_var,
  time_var,
  decision_var,
  group_var = NULL,
  cluster_var = NULL,
  focal_value = 1,
  output_file
)
}
\arguments{
\item{id_var}{Character scalar. ID variable name.}

\item{time_var}{Character scalar. Time variable name.}

\item{decision_var}{Character scalar. Decision variable name.}

\item{group_var}{Character scalar or \code{NULL}. Group variable name.
Default \code{NULL}.}

\item{cluster_var}{Character scalar or \code{NULL}. Cluster variable name.
Default \code{NULL}.}

\item{focal_value}{Numeric scalar. Focal decision value for gap computation.
Default \code{1}.}

\item{output_file}{Character scalar. Path to write the \code{.R} script.}
}
\value{
Invisibly returns \code{output_file}.
}
\description{
Writes a self-contained R script to \code{output_file} that replicates the
analysis performed in the RobustFlow Shiny application with the given
variable mappings. The generated script is intended as a starting point
for users who want to reproduce or extend the app analysis programmatically.
}
\examples{
tmp <- tempfile(fileext = ".R")
generate_r_script(
  id_var       = "child_id",
  time_var     = "wave",
  decision_var = "risk_math",
  group_var    = "ses_group",
  focal_value  = 1,
  output_file  = tmp
)
# Show first 10 lines
cat(readLines(tmp, n = 10), sep = "\n")
unlink(tmp)
}
