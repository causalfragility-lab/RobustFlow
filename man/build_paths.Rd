% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_paths.R
\name{build_paths}
\alias{build_paths}
\title{Construct individual decision paths and the aggregate transition matrix}
\usage{
build_paths(data, id, time, decision, sep = "->")
}
\arguments{
\item{data}{A data frame in long format, pre-sorted by \code{id} and \code{time}
(e.g., the \code{data} element returned by \code{\link[=validate_panel_data]{validate_panel_data()}}).}

\item{id}{Character scalar. Name of the individual identifier variable.}

\item{time}{Character scalar. Name of the time variable.}

\item{decision}{Character scalar. Name of the decision or outcome variable.}

\item{sep}{Character scalar. Separator inserted between consecutive decision
states in the path string. Default \code{"->"}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{individual_paths}}{Data frame with one row per individual and
columns \code{id} and \code{path}.}
\item{\code{path_counts}}{Data frame of unique paths with columns \code{path},
\code{n} (frequency), and \code{pct} (percentage), sorted in descending order
of frequency.}
\item{\code{transition_matrix}}{Integer matrix of pooled transition counts
(rows = from-state, columns = to-state).}
\item{\code{path_entropy}}{Numeric scalar. Shannon entropy (bits) of the path
frequency distribution. Higher values indicate greater diversity of
individual trajectories.}
}
}
\description{
For each individual, concatenates their sequence of decisions (or outcomes)
over time into a single path string. Returns individual-level paths,
aggregate frequency counts, the pooled transition matrix, and path entropy.
}
\examples{
df <- data.frame(
  id   = rep(1:4, each = 3),
  time = rep(1:3, times = 4),
  dec  = c(0L, 1L, 1L, 1L, 1L, 0L, 0L, 0L, 1L, 1L, 0L, 0L)
)
result <- build_paths(df, id = "id", time = "time", decision = "dec")
head(result$path_counts)
result$transition_matrix
result$path_entropy
}
