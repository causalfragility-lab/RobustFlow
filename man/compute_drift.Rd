% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics_dii.R
\name{compute_drift}
\alias{compute_drift}
\title{Compute the Drift Intensity Index (DII) over time}
\usage{
compute_drift(data, id, time, decision, normalize = TRUE)
}
\arguments{
\item{data}{A data frame in long format.}

\item{id}{Character scalar. Name of the individual identifier variable.}

\item{time}{Character scalar. Name of the time variable.}

\item{decision}{Character scalar. Name of the decision or outcome variable.}

\item{normalize}{Logical. If \code{TRUE} (default), each transition matrix is
row-normalized to proportions before computing the Frobenius norm. If
\code{FALSE}, raw counts are used.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{summary}}{Data frame with columns \code{time} and \code{DII}. The first
time point always has \code{DII = NA} (no preceding period).}
\item{\code{matrices}}{Named list of period-specific transition matrices
(one per time interval).}
\item{\code{mean_dii}}{Numeric scalar. Mean DII across all non-missing
periods.}
\item{\code{max_dii_period}}{The time value at which DII is largest.}
}
}
\description{
The DII quantifies structural instability in the decision transition system
between consecutive time periods. For each pair of adjacent time points
\eqn{(t-1, t)}, a period-specific transition matrix \eqn{P_t} is estimated
from observed consecutive-state pairs, and the DII is defined as the
Frobenius norm of their difference:
}
\details{
\deqn{DII_t = \| P_t - P_{t-1} \|_F = \sqrt{\sum_{i,j}(P_t^{ij} - P_{t-1}^{ij})^2}}

When \code{normalize = TRUE} (default), each matrix is row-normalized before
computing the norm, so DII is scale-free and comparable across datasets.

The period-specific transition matrix \eqn{P_t} is constructed from
transitions observed \emph{between} time \eqn{t-1} and time \eqn{t} only
(not cumulatively). Individuals present at both \eqn{t-1} and \eqn{t}
contribute one transition pair. Individuals missing at either wave are
excluded from that period's matrix.
}
\examples{
set.seed(42)
df <- data.frame(
  id   = rep(seq_len(50), each = 4),
  time = rep(seq_len(4), times = 50),
  dec  = sample(0:1, 200, replace = TRUE)
)
result <- compute_drift(df, id = "id", time = "time", decision = "dec")
result$summary
result$mean_dii
}
\references{
Hait, S. (2025). \emph{RobustFlow: Robustness and drift auditing for
longitudinal decision systems}. R package version 0.1.0.
}
