% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_validation.R
\name{validate_panel_data}
\alias{validate_panel_data}
\title{Validate and prepare longitudinal panel data}
\usage{
validate_panel_data(data, id, time, decision, group = NULL, cluster = NULL)
}
\arguments{
\item{data}{A data frame in long format (one row per individual per time
point).}

\item{id}{Character scalar. Name of the individual identifier variable.}

\item{time}{Character scalar. Name of the time variable.}

\item{decision}{Character scalar. Name of the decision or outcome variable.}

\item{group}{Character scalar or \code{NULL}. Optional name of the subgroup
variable (e.g., SES group, race/ethnicity). Default \code{NULL}.}

\item{cluster}{Character scalar or \code{NULL}. Optional name of a cluster
variable (e.g., school, site). Default \code{NULL}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{data}}{Sorted data frame (by \code{id}, then \code{time}).}
\item{\code{n_ids}}{Integer. Number of unique individuals.}
\item{\code{n_times}}{Integer. Number of unique time points.}
\item{\code{balanced}}{Logical. \code{TRUE} if every individual appears at every
time point (balanced panel).}
\item{\code{missingness}}{Named integer vector. Count of \code{NA} values for each
required variable.}
}
}
\description{
Checks that required variables exist in \code{data}, sorts the data by
individual and time, and returns a structured list with diagnostic
information about the panel.
}
\examples{
df <- data.frame(
  child_id = rep(1:5, each = 3),
  wave     = rep(1:3, times = 5),
  outcome  = sample(0:1, 15, replace = TRUE)
)
result <- validate_panel_data(
  data     = df,
  id       = "child_id",
  time     = "wave",
  decision = "outcome"
)
result$n_ids
result$balanced
}
