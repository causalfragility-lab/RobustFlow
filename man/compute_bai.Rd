% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics_bai.R
\name{compute_bai}
\alias{compute_bai}
\title{Compute the Bias Amplification Index (BAI)}
\usage{
compute_bai(gap_series, standardize = FALSE, threshold = 0.05)
}
\arguments{
\item{gap_series}{Numeric vector of gap values, one per time point, ordered
chronologically. \code{NA} values are removed before computation.}

\item{standardize}{Logical. If \code{TRUE}, returns the standardized BAI
(divided by the SD of \code{gap_series}). Default \code{FALSE}.}

\item{threshold}{Numeric scalar. Absolute BAI threshold used to classify
the direction as amplification or convergence. Values with
\eqn{|BAI| \le} \code{threshold} are classified as "stable".
Default \code{0.05}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{bai}}{Numeric scalar. The (optionally standardized) BAI.}
\item{\code{gap_start}}{Gap at the first time point.}
\item{\code{gap_end}}{Gap at the last time point.}
\item{\code{direction}}{Character. \code{"amplification"}, \code{"convergence"}, or
\code{"stable"}.}
}
}
\description{
The BAI measures whether the disparity gap between two groups widens or
narrows from the first to the last observed time point. It is defined as:
}
\details{
\deqn{BAI = Gap_T - Gap_1}

A positive BAI indicates amplification (widening gap); a negative BAI
indicates convergence (narrowing gap); values near zero indicate stability.

An optional standardized version divides by the standard deviation of the
gap series:

\deqn{BAI^* = \frac{Gap_T - Gap_1}{SD(Gap_t)}}
}
\examples{
# Widening gap over 5 waves
gaps <- c(0.10, 0.12, 0.15, 0.18, 0.22)
compute_bai(gaps)

# Narrowing gap
compute_bai(c(0.20, 0.15, 0.10, 0.05))

# Standardized
compute_bai(gaps, standardize = TRUE)
}
